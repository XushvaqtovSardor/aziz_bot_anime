services:
  db:
    image: postgres:16
    container_name: aziz_animedb
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: aziz_grammydb
      # PostgreSQL optimizatsiya sozlamalari
      POSTGRES_INITDB_ARGS: "-E UTF8"
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5433:5432"
    volumes:
      - baza_data:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql/postgresql.conf:ro
    command: 
      - "postgres"
      - "-c"
      - "config_file=/etc/postgresql/postgresql.conf"
    # CPU va Memory limitlari - yuqori ishlatishni oldini olish uchun
    deploy:
      resources:
        limits:
          cpus: '0.6'      # Maksimal 0.6 CPU core (1-CPU server uchun)
          memory: 1G       # Maksimal 1GB RAM
        reservations:
          cpus: '0.2'      # Minimal 0.2 CPU
          memory: 512M     # Minimal 512MB RAM
    # Healthcheck yaxshilandi
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aziz_grammydb"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  app:
    build: .
    container_name: aziz_anime
    restart: always
    env_file:
      - .env
    expose:
      - "3001"
    depends_on:
      db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '0.7'
          memory: 512M
        reservations:
          cpus: '0.15'
          memory: 256M
    command: >
      sh -c "
        pnpm run build
        npx prisma generate &&
        npx prisma migrate deploy &&
        pnpm run start:prod
      "


volumes:
  baza_data:
